'use strict';

describe('Service: interappMessaging', function () {

  // // load the service's module
  // beforeEach(module('headlessDockerApp'));

  // // instantiate service
  // var interappMessaging;
  // beforeEach(inject(function (_interappMessaging_) {
  //   interappMessaging = _interappMessaging_;
  // }));

  // it('should expose a send function', function () {
  //   expect(interappMessaging.send).toBeDefined();
  // });
  // it('should expose a publish function', function () {
  //   expect(interappMessaging.publish).toBeDefined();
  // });
  // it('should expose a subscribe function', function () {
  //   expect(interappMessaging.subscribe).toBeDefined();
  // });

  // it('should expose a listSubscribedApplications function', function () {
  //   expect(interappMessaging.listSubscribedApplications).toBeDefined();
  // });

});
